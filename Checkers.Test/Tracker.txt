POSSIBLE IMPROVEMENTS:

* 1. Create a Coordinate class - int array
* 2. Create a Coordinate field for a checker
* 3. Extend ExcludeNontakableEnemies() for a Queen search
* 4. CanSelectChecker проверяет что у шашки есть куда ходить - она не заблокирована другими шашками или границам поля
* 5. GetChecker() добавлен, принимает координату и возвращает шашку - чтобы избегать промежуточного мансирования с GetCheckerId()
* 6. SwitchPlayer() приукрасил

7. добавить строку, где будет отображаться общий текущий ход игрока, например: "C5-D6" или "C5-E7-F5" и этот текст будет обновляться по мере ввода
...
99. подумать о low coupling - high cohesion
==============

CURRENT ISSUES:

1. Убрать координаты из шашки. Достаточно координат клетки. Клетка и есть координата. (?)
2. А длинный ход (compound move) еще работает? надо бы его не забыть учесть!

==============

SOLVED ISSUES:

* Когда Player1 выбирает вместо своей шашки пустую клетку чтобы походить,то игра не сообщает об ошибке, а вместо этого ходит на указанную далее клетку шакой с id = 0;
* Сделать проверку, что нельзя переходить в уже занятую клетку (сейчас - можно =), шашка напрыгивает на находящуюся там шашку) => update @ 24.06.2015 - Добавил проверку isCellUsed в CanSelelectChecker для обычной шашки
* Лямбда-выражения вместо циклов foreach в методах определения врагов - AS: попробовали, оно не стоит того =)
* Сделать проверку в самом начале хода (ещё до выбора шашки) на наличие вариантов боя у какой-либо из шашек. Если бой есть - разрешать выбрать только шашку с боем.
* Допилить CanMoveThere() на правильный перебор вариантов возможного хода при бое
* int[]adressOld и int[]adressNew везде заменены на Сoordinate
* Допилить метод CanSelect() - если у шашки некуда походить - ее нельзя выбирать! - заготовка метода готова, надо универсализовать!
* Переименовать addressOld и addressNew на чето не тупое: adressOld => moveStartCoordinate, adressNew => moveEndCoordinate: АЛЬТЕРНАТИВНЫЕ ВАРИАНТЫ ПРИВЕТСТВУЮТСЯ!!!
* PossibleTakes.Clear() добавлен в Main - был баг, когда две шашки видят перед собой одного врага, то одна из них могла "бить" по старой памяти шашку, которая из под боя уже ушла или была побита.
* Допилить метод FindPossibleTakes() на предмет "2 занятые клетки подряд" - дамка все еще может прыгать через два врага!!! - исправлено, я забыл сначала правильно учесть границы поля
* Избавился от всех использований GetCheckerId() везде. Теперь там используется GetChecker(). Собственно от самого GetCheckerId() тоже избавился 
==============



============== Вопросы к Кириллу

1. В Locals и в Watch разные значения поля!!11111
2. Как правильно сравнить объекты в юнит тестах?
3. Надо ли выносить координаты из шашки?
4. Как быть с модификаторами доступа к методам, если мы хотим их тестировать?
5. Откуда Board заранее знает IsUsable?! Еще до инициализации поля уже проставлены.
6. if vs. continue (to reduce nesting)?
7. Is do/while ok?