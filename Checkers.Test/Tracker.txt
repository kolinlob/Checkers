POSSIBLE IMPROVEMENTS:

* 1. Create a Coordinate class - int array
2. Create a Coordinate field for a checker
3. Extend ExcludeNontakableEnemies() for a Queen search

KNOWN ISSUES:

1. Когда Player1 выбирает вместо своей шашки пустую клетку чтобы походить,
   то игра не сообщает об ошибке, а вместо этого ходит на указанную далее клетку шакой с id = 0;

   update
   В методе Game -> GetCheckerId() изменил с First() на Single();
   Теперь при выборе пустой клетки, в конце хода выдается исключение. Но все еще нужно добавить проверку, чтобы прямо в момент ввода сообщалось что выбор некорректный
