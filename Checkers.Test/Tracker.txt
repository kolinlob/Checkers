POSSIBLE IMPROVEMENTS:

* 1. Create a Coordinate class - int array
2. Create a Coordinate field for a checker
3. Extend ExcludeNontakableEnemies() for a Queen search
4. надо добавить проверку в game.CanSelectChecker, что у шашки есть куда ходить - она не заблокирована другими шашками или границам поля
5. добавить строку, где будет отображаться общий текущий ход игрока, например: "C5-D6" или "C5-E7-F5" и этот текст будет обновляться по мере ввода

KNOWN ISSUES:

*  1. [ИСПРАВЛЕНО]
   Когда Player1 выбирает вместо своей шашки пустую клетку чтобы походить,
   то игра не сообщает об ошибке, а вместо этого ходит на указанную далее клетку шакой с id = 0;

   РЕШЕНИЕ
   В методе Game -> GetCheckerId() изменил с First() на Single();
   Теперь при выборе пустой клетки, в конце хода выдается исключение. Но все еще нужно добавить проверку, чтобы прямо в момент ввода сообщалось что выбор некорректный

   update @ 30.05.2015
   в методы GetCheckerId() и CanSelelectChecker() добавлены try - catch: теперь при неправильном выборе GetCheckerId возвращает -1, а CanSelect в таком случае дает false.

* Сделать проверку, что нельзя переходить в уже занятую клетку (сейчас - можно =), шашка напрыгивает на находящуюся там шашку)
  update @ 24.06.2015 - Добавил проверку isCellUsed в CanSelelectChecker для обычной шашки

* Лямбда-выражения вместо циклов foreach в методах определения врагов - AS: попробовали, оно не стоит того =)
* Сделать проверку в самом начале хода (ещё до выбора шашки) на наличие вариантов боя у какой-либо из шашек. Если бой есть - разрешать выбрать только шашку с боем.
* Допилить CanMoveThere() на правильный перебор вариантов возможного хода при бое

==============

Методы CanMoveThere() и MoveChecker() не трогать; они должны сработать уже после отработки проверки на наличие боя. Возможно, внедрить переменную bool isTakeAvailable?
Переименовать addressOld и addressNew на чето не тупое - может от них вообще избавиться и переделать все adressXXX в coordinate?
Убрать координаты из шашки. Достаточно координат клетки. Клетка и есть координата.
Допилить метод FindPossibleTakes() на предмет "2 занятые клетки подряд" - он должен становиться встретив такое!
Допилить метод CanSelect() - если у шашки некуда походить - ее нельзя выбирать!

for (var k = 1; k <= end; k++) - добавить IsQueen - это еще актуально?

============== Вопросы к Кириллу

1. В Locals и в Watch разные значения поля!!11111
2. Как правильно сравнить объекты в юнит тестах?
3. Надо ли выносить координаты из шашки?
4. Как быть с модификаторами доступа к методам, если мы хотим их тестировать?
5. Откуда Board заранее знает IsUsable?! Еще до инициализации поля уже проставлены.
6. if vs. continue (to reduce nesting)?
7. Is do/while ok?
